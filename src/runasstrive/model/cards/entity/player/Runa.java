package runasstrive.model.cards.entity.player;

import runasstrive.model.Level;
import runasstrive.model.cards.entity.type.CharacterType;

public class Runa extends Player {
    public Runa(Level level, int hp) {
        super(level, hp);
    }
}
